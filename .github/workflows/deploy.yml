name: Local Deployment

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted  

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Build or Deployment Script
        run: |
          echo "Deploying directly on my local machine..."
          
